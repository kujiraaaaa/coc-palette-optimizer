# GitHub Actionsの名前
name: Deploy Vite App to GitHub Pages

# このワークフローが実行されるタイミングを指定
on:
  # mainブランチにプッシュされた時に実行
  push:
    branches: [ "main" ]
  # Actionsタブから手動で実行することも可能にする
  workflow_dispatch:

# ワークフローの実行に必要な権限を設定
permissions:
  contents: read
  pages: write
  id-token: write

# ワークフローの実行環境を指定
jobs:
  # 'build'という名前のジョブ
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. リポジトリのコードをチェックアウト
      - name: Checkout
        uses: actions/checkout@v4
      
      # 2. Node.js環境をセットアップ
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      
      # 3. 依存関係をインストール
      - name: Install dependencies
        run: npm ci
      
      # 4. プロジェクトをビルド（distフォルダが作られる）
      - name: Build
        run: npm run build
        
      # 5. ビルド成果物（distフォルダ）をアップロードする準備
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # 6. ビルド成果物をアップロードして、次の'deploy'ジョブで使えるようにする
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  # 'deploy'という名前のジョブ
  deploy:
    # 'build'ジョブが成功したら実行される
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # 7. アップロードされた成果物をGitHub Pagesにデプロイする
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
